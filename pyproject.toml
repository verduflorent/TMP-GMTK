[tool.poetry]
name = "tmp-gmtk"
version = "0.1.0"
description = "THE MOIRA PROTOCOL — Game Master Toolkit"
authors = ["Florent Verdu"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = ">=3.14,<3.15"
django = "6.1.1"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
